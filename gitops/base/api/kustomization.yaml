apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# commonLabels 대체: selector에 주입하지 않는 labels 사용
labels:
  - pairs:
      app.kubernetes.io/part-of: docflow
    includeSelectors: false
